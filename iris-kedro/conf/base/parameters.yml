# Train / Test
train_test_split:
  test_size: 0.2
  random_state: 42

# KNN
knn_model:
  n_neighbors: 5

# Logistic Regression
logreg_model:
  max_iter: 200

# SVM
svm_model:
  C: 1.0
  kernel: rbf

# Random Forest
rf_model:
  n_estimators: 150
  random_state: 42
  max_depth: 5
